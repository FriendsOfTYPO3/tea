---
stages:
  - build
  - test

include:
  - template: License-Management.gitlab-ci.yml
  - template: SAST.gitlab-ci.yml
  - template: Code-Quality.gitlab-ci.yml

default:
  image: php:7.4
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null

variables:
  MYSQL_ROOT_PASSWORD: root
  typo3DatabaseName: typo3
  typo3DatabaseHost: mariadb
  typo3DatabaseUsername: root
  typo3DatabasePassword: root

build-composer-dependencies:
  stage: build
  variables:
    COMPOSER_CACHE_DIR: '.composer'
  script:
    - COMPOSER_CACHE_DIR=.composer
      composer install --prefer-dist --no-progress --optimize-autoloader
  artifacts:
    when: on_success
    expire_in: 2 weeks
    paths:
      - .Build
  cache:
    paths:
      - .composer
      - .Build

unit-tests-php7.4-t3-v10:
  stage: test
  script:
    - composer ci:tests:unit
  allow_failure: true

unit-tests-php7.3-t3-v10:
  image: php:7.3
  stage: test
  script:
    - composer ci:tests:unit
  allow_failure: true

unit-tests-php7.2-t3-v10:
  image: php:7.2
  stage: test
  script:
    - composer ci:tests:unit
  allow_failure: true

functional-tests-php7.4-t3-v10:
  services:
    - mariadb:10
  stage: test
  script:
    - composer ci:tests:functional
  allow_failure: true

functional-tests-php7.3-t3-v10:
  image: php:7.3
  services:
    - mariadb:10
  stage: test
  script:
    - composer ci:tests:functional
  allow_failure: true

functional-tests-php7.2-t3-v10:
  image: php:7.2
  services:
    - mariadb:10
  stage: test
  script:
    - composer ci:tests:functional
  allow_failure: true

unit-tests-php7.4-t3-v9:
  image: php:7.4
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:unit
  allow_failure: true

unit-tests-php7.3-t3-v9:
  image: php:7.3
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:unit
  allow_failure: true

unit-tests-php7.2-t3-v9:
  image: php:7.2
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:unit
  allow_failure: true

functional-tests-php7.4-t3-v9:
  image: php:7.4
  services:
    - mariadb:10
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:functional
  allow_failure: true

functional-tests-php7.3-t3-v9:
  image: php:7.3
  services:
    - mariadb:10
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:functional
  allow_failure: true

functional-tests-php7.2-t3-v9:
  image: php:7.2
  services:
    - mariadb:10
  stage: test
  before_script:
    - bash .gitlab/build/docker_install.sh > /dev/null
    - composer require typo3/cms-core:"^9.5"
  script:
    - composer ci:tests:functional
  allow_failure: true

code_quality:
  before_script: []

eslint-sast:
  before_script: []

phpcs-security-audit-sast:
  before_script: []

secrets-sast:
  before_script: []
